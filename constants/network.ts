export const NETWORK_CONFIG = {
  DISCOVERY_PORT: 3000,
  UDP_BROADCAST_PORT: 3000,
  TCP_MESSAGE_PORT: 3001,
  TCP_FILE_PORT: 3002,
  DISCOVERY_INTERVAL: 5000,
  HEARTBEAT_INTERVAL: 3000,
  USER_TIMEOUT: 10000,
  BROADCAST_ADDRESS: "255.255.255.255",
  FILE_CHUNK_SIZE: 64 * 1024,
} as const;
